/**
 * 화면 오른쪽 위에 찍히는 판 번호.
 *
 * 왜 필요한가 — 밀어 넣은 것이 아이패드까지 갔는지 확인할 방법이 없었다.
 * 서비스 워커가 옛 파일을 붙들고 있으면 고친 게 안 보이는데, 그때 화면만
 * 봐서는 "안 고쳐졌다"와 "옛 판을 보고 있다"를 구분할 수 없다.
 *
 * **고칠 때마다 하나씩 올린다.** 화면의 숫자와 여기 숫자가 같으면 최신이다.
 */
export const VERSION = 'v31';
